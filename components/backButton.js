import { View, Text, TouchableOpacity } from 'react-native'
import React from 'react'

const BackButton = () => {
  return (
    <TouchableOpacity>        
      <Text style={{color:'black'}}>BackButton</Text>
    </TouchableOpacity>
  )
}

export default BackButton